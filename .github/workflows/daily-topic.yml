# 已废弃，请使用 daily-push.yml
# 仅保留 workflow_dispatch 手动触发
name: 每日推送 (旧)

on:
  workflow_dispatch:

jobs:
  morning-topics:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install requests
      - run: echo "已废弃，请去 daily-push.yml 查看新流程"